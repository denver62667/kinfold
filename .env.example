# --- server ---
PORT=3000
# NODE_ENV=production   # set in production (enables proxy trust behind a load balancer)

# Kinfold's sources (Open Archives, Chronicling America, Internet Archive) are all
# free and need NO API key — the app works out of the box with no secrets to set.
